services:
  marte:
    build:
      context: .
      dockerfile: docker/Dockerfile.local
    container_name: marte
    ports:
      - "14100:14100"
      - "14101:14101"
    volumes:
      - ./data:/app
    restart: unless-stopped
